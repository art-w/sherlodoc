(rule
 (alias base_odocls)
 (target odocls.tar)
 (action
  (progn
   (run mkdir -p docs)
   (run odig odoc --cache-dir=docs base)
   (run tar -cf odocls.tar docs/odoc))))

(cram
 (deps
  (alias base_odocls)
  odocls.tar
  %{bin:odoc}
  %{bin:sherlodoc}
  ../../jsoo/main.bc.js))
